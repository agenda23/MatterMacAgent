{
  "name": "@matter-mac-agent/sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --noEmit && node scripts/bundle.js",
    "build:stub": "tsc --noEmit && node scripts/bundle.js --stub",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@matter/main": "^0.12.0",
    "@matter/nodejs": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "~5.8.3"
  }
}
